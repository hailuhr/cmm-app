<!--Navbar & Menu Dropdown-->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
<!-- <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown">
    <span class="navbar-toggler-icon"></span>
</button> -->
<div id="navbarNavDropdown" class="navbar-collapse collapse">
    <ul class="navbar-nav mr-auto">
      <% if current_user %>
        <li class="nav-item"><%= link_to 'Home', root_path, :class => "nav-link" %></li>
        <% if current_user.profile.nil? %>
          <li class="nav-item"><%= link_to 'Profile', new_user_profile_path(current_user) , :class => "nav-link" %></li>
        <% else %>
          <li class="nav-item"><%= link_to 'Profile', user_profile_path(current_user, current_user.profile) , :class => "nav-link" %></li>
        <% end %>
      <% else %>
        <li class="nav-item"><%= link_to 'CMD Magic', new_user_registration_path, :class => "navbar-brand" %></li>
      <% end %>
    </ul>
    <ul class="navbar-nav">
        <li class="dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown">
              Menu
            </a>
            <div class="dropdown-menu dropdown-menu-right">
              <% if current_user %>
                <% if current_user.profile.nil? %>
                  <%= link_to 'Create Profile', new_user_profile_path(current_user) , :class => "dropdown-item" %>
                <% else %>
                  <%= link_to 'Edit Profile', edit_user_profile_path(current_user, current_user.profile.id) , :class => "dropdown-item" %>
                <% end %>
                <div class="dropdown-divider"></div>
                <%= link_to 'Logout', destroy_user_session_path, :method => :delete, :class => "dropdown-item" %>
              <% else %>
                <%= link_to 'Login', new_user_session_path, :class => "dropdown-item" %>
                <div class="dropdown-divider"></div>
                <%= link_to 'Register', new_user_registration_path, :class => "dropdown-item" %>
              <% end %>
            </div>
        </li>
    </ul>
</div>
</nav>
